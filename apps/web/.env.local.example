# Leave empty for combined single-service deployment (frontend + backend +
# nginx together) -- requests go to the relative /api path, same origin.
#
# Set to http://localhost:8000 for local dev with two separate servers
# (npm run dev on :3000, uvicorn on :8000).
NEXT_PUBLIC_API_URL=
